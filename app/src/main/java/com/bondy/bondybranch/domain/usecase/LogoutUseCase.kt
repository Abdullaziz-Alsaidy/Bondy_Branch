package com.bondy.bondybranch.domain.usecase

import javax.inject.Inject

class LogoutUseCase @Inject constructor() {
    operator fun invoke() {
        // TODO: hook into session storage when implemented
    }
}
